import React from 'react';
import DropdownList from './DropdownList';

const App: React.FC = () => {
  return (
    <div>
      <DropdownList /> {/* DropdownListコンポーネントをここで使用 */}
    </div>
  );
};

export default App;
